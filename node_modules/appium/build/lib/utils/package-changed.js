"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.packageDidChange = packageDidChange;
const node_path_1 = __importDefault(require("node:path"));
const support_1 = require("@appium/support");
const constants_1 = require("../constants");
const logger_1 = require("../logger");
const is_package_changed_1 = require("./is-package-changed");
/**
 * Determines if extensions have changed, and updates a hash the `package.json` in `appiumHome` if so.
 *
 * If they have, we need to sync them with the `extensions.yaml` manifest.
 */
async function packageDidChange(appiumHome) {
    const hashFilename = node_path_1.default.join(appiumHome, constants_1.PKG_HASHFILE_RELATIVE_PATH);
    const hashFilenameDir = node_path_1.default.dirname(hashFilename);
    logger_1.log.debug(`Creating hash file directory: ${hashFilenameDir}`);
    try {
        await support_1.fs.mkdirp(hashFilenameDir);
    }
    catch (err) {
        throw new Error(`Appium could not create the directory for hash file: ${hashFilenameDir}. Original error: ${err.message}`, { cause: err });
    }
    let isChanged;
    let writeHash;
    let oldHash;
    let hash;
    try {
        ({ isChanged, writeHash, oldHash, hash } = await (0, is_package_changed_1.isPackageChanged)({
            cwd: appiumHome,
            hashFilename: constants_1.PKG_HASHFILE_RELATIVE_PATH,
        }));
    }
    catch {
        // If the library fails, assume the manifest may be stale and should be refreshed.
        return true;
    }
    if (isChanged) {
        try {
            await writeHash();
            logger_1.log.debug(`Updated hash of ${appiumHome}/package.json from: ${oldHash ?? '(none)'} to: ${hash}`);
        }
        catch (err) {
            throw new Error(`Appium could not write hash file: ${hashFilenameDir}. Original error: ${err.message}`, {
                cause: err,
            });
        }
    }
    return isChanged;
}
//# sourceMappingURL=package-changed.js.map