import { IAzureConfig } from './interface';
import { Frameworks, Services } from '@wdio/types';
import { ITestCaseHookParameter } from '@cucumber/cucumber';
import { Test, TestResult } from '@wdio/types/build/Frameworks';
export default class AzureDevopsService implements Services.ServiceInstance {
    private readonly _options;
    private readonly _azureReporter;
    constructor(_options: IAzureConfig);
    onPrepare(): Promise<void>;
    onComplete(): Promise<void>;
    afterTest(test: Test, context: any, result: TestResult): Promise<void>;
    afterScenario(world: ITestCaseHookParameter, result: Frameworks.PickleResult): Promise<void>;
    private parseCaseID;
    private parseCaseIDString;
}
